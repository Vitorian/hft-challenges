title: "Challenge 13: Implied Book"
slug: "implied-book"
directory_name: "challenge-13-implied-book"
description_md: |
  ## Implied Order Book

  Futures exchanges trade calendar spreads (Jun-Jul), butterflies (Jun-2×Jul+Aug),
  and strips (Apr+May+Jun). The implied order book for a synthetic instrument is
  computed from the outright books of its legs.

  ### The Problem

  You manage outright L2 order books (5 levels per side) for each leg of a synthetic.
  When any leg's book changes, you must recompute the top 5 implied bid and ask levels.

  For a synthetic defined by weights `[w₁, w₂, ..., wₙ]`:
  - **Implied bid price** = Σ(wᵢ × leg_priceᵢ), choosing ask levels for legs you buy (w>0)
    and bid levels for legs you sell (w<0)
  - **Implied quantity** = min(leg_qtyᵢ / |wᵢ|) across all legs
  - **Implied ask** = the inverse: bid levels for w>0, ask levels for w<0

  The naive approach enumerates all D^N level combinations (5³ = 125 for 3 legs).
  Incremental approaches that only recompute affected combinations can be much faster.

  ### Example: Calendar Spread (Jun - Jul)

  Weights: `{+1, -1}` — buy June, sell July.

  **Outright books:**

  ```
  June (Leg 0):              July (Leg 1):
  Bid        Ask             Bid        Ask
  50 @ 100   30 @ 103        40 @ 200   25 @ 204
  80 @ 99    60 @ 104        70 @ 199   50 @ 205
  ```

  **Implied bid** (sell the spread to us = sell June bid, buy July ask):
  - Someone selling the spread receives the June bid and pays the July ask.
  - Price = (+1 × June_bid) + (-1 × July_ask) = June_bid - July_ask

  ```
  Step 1: Best June bid (50 @ 100) vs best July ask (25 @ 204)
    Price = 100 - 204 = -104, Qty = min(50, 25) = 25
    Remaining: June bid has 25 left @ 100. July ask exhausted → next is 50 @ 205.

  Step 2: Best June bid (25 @ 100) vs best July ask (50 @ 205)
    Price = 100 - 205 = -105, Qty = min(25, 50) = 25
    Remaining: June bid exhausted → next is 80 @ 99. July ask has 25 left @ 205.

  Step 3: Best June bid (80 @ 99) vs best July ask (25 @ 205)
    Price = 99 - 205 = -106, Qty = min(80, 25) = 25
    Remaining: June bid has 55 left @ 99. July ask exhausted. Stop.
  ```

  Result — implied bids (best first):

  ```
  25 @ -104
  25 @ -105
  25 @ -106
  ```

  **Implied ask** (buy the spread from us = buy June ask, sell July bid):
  - Someone buying the spread pays the June ask and receives the July bid.
  - Price = (+1 × June_ask) + (-1 × July_bid) = June_ask - July_bid

  ```
  Step 1: Best June ask (30 @ 103) vs best July bid (40 @ 200)
    Price = 103 - 200 = -97, Qty = min(30, 40) = 30
    Remaining: June ask exhausted → next is 60 @ 104. July bid has 10 left @ 200.

  Step 2: Best June ask (60 @ 104) vs best July bid (10 @ 200)
    Price = 104 - 200 = -96, Qty = min(60, 10) = 10
    Remaining: June ask has 50 left @ 104. July bid exhausted → next is 70 @ 199.

  Step 3: Best June ask (50 @ 104) vs best July bid (70 @ 199)
    Price = 104 - 199 = -95, Qty = min(50, 70) = 50
    Remaining: June ask exhausted. Stop.
  ```

  Result — implied asks (best first):

  ```
  30 @ -97
  10 @ -96
  50 @ -95
  ```

  The implied spread market is -104 / -97 (7 ticks wide).
  Bids (-104 to -106) are below asks (-95 to -97), as expected.
  The synthetic spread is wider than either outright because you pay
  both outright bid-ask spreads when constructing the synthetic.

  Now imagine this recomputation happening on every single book update, thousands
  of times per second. With 3 legs and 5 levels, that produces up to 3×5 = 15
  sweep steps per side per update.

  ### Interface

  ```cpp
  struct Level {
      int64_t price;
      int64_t quantity;
  };

  struct BookUpdate {
      uint16_t leg_index;   // which leg changed
      int8_t   side;        // 0=bid, 1=ask
      uint8_t  action;      // 0=add, 1=modify, 2=delete
      Level    level;        // price + quantity
  };

  class ImpliedBook {
  public:
      // Define synthetic: weights per leg, output depth. NOT timed.
      void build(std::span<const int> weights, int depth);

      // Apply outright book update, return top implied levels.
      // out_bids/out_asks pre-allocated with `depth` capacity.
      std::pair<int,int> on_update(const BookUpdate& update,
                                   Level* out_bids, Level* out_asks);
  };
  ```

  ### Rules

  - `build()` is called once before each benchmark run. Use it to pre-allocate.
  - `on_update()` must apply the update to the outright book AND return the
    current top implied levels for both sides.
  - Output levels must be sorted: bids descending, asks ascending by price.
  - Same-price levels from different combinations must be aggregated (quantities summed).
  - `out_bids` and `out_asks` have capacity for `depth` levels.

  ### Benchmarks

  Three workloads:

  - **butterfly_3leg**: Weights {+1, -2, +1}, 5 levels — classic butterfly
  - **spread_2leg**: Weights {+1, -1}, 5 levels — calendar spread
  - **strip_3leg**: Weights {+1, +1, +1}, 5 levels — strip

  ### Metric

  Cycles per update (p99). Lower is better.

time_limit_seconds: 30
memory_limit_mb: 512
daily_submission_limit: 10
published: true

goals:
  - name: "fastest"
    display_name: "Fastest Overall"
    description: "Average cycles/op across all workloads"
    aggregation: "average"
    benchmarks: ["butterfly_3leg", "spread_2leg", "strip_3leg"]
    order: "asc"
    unit: "cycles/op"
  - name: "butterfly"
    display_name: "Best Butterfly"
    description: "Performance on 3-leg butterfly {+1,-2,+1}"
    aggregation: "single"
    benchmarks: ["butterfly_3leg"]
    order: "asc"
    unit: "cycles/op"
  - name: "spread"
    display_name: "Best Spread"
    description: "Performance on 2-leg calendar spread"
    aggregation: "single"
    benchmarks: ["spread_2leg"]
    order: "asc"
    unit: "cycles/op"

badges:
  gold: 50
  silver: 150
  bronze: 500
